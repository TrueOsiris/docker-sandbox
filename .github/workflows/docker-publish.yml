# .github/workflows/docker-publish.yml
# Author: Tim Chaubet

name: Build and Push Sandbox Image

on:
  push:
    branches: [ "main" ]
    paths:
      - 'Dockerfile'
      - 'scripts/**'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Lowercase Registry Path
        id: repo
        run: |
          echo "owner=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_OUTPUT

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.REGISTRY }}/${{ steps.repo.outputs.owner }}/sandbox
            ${{ env.REGISTRY }}/${{ steps.repo.outputs.owner }}/docker-sandbox

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ steps.repo.outputs.owner }}/sandbox:latest
            ${{ env.REGISTRY }}/${{ steps.repo.outputs.owner }}/docker-sandbox:latest
            ${{ env.REGISTRY }}/${{ steps.repo.outputs.owner }}/sandbox:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ steps.repo.outputs.owner }}/docker-sandbox:${{ github.sha }}
          labels: ${{ steps.meta.outputs.labels }}
          pull: true